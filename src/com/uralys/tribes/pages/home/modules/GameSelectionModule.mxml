<?xml version="1.0" encoding="utf-8"?>
<mx:Module xmlns:fx="http://ns.adobe.com/mxml/2009" 
		   xmlns:s="library://ns.adobe.com/flex/spark" 
		   xmlns:mx="library://ns.adobe.com/flex/mx" 
		   xmlns:custom="com.uralys.renderers.*"
		   layout="absolute"
		   implements="com.uralys.tribes.pages.home.modules._IGameSelectionModule"
		   width="100%" height="100%"
		   initialize="init()" xmlns:renderers="com.uralys.tribes.renderers.*">
	
	<fx:Script>
		<![CDATA[
			import com.uralys.tribes.commons.Session;
			import com.uralys.tribes.commons.Translations;
			import com.uralys.tribes.entities.Game;
			import com.uralys.tribes.managers.GameManager;
			
			import mx.collections.ArrayCollection;
			import mx.events.FlexEvent;
			import mx.events.ItemClickEvent;
			import mx.utils.ObjectUtil;
			
			//----------------------------------------------------------//
			
			[Bindable] private var games:ArrayCollection;
			[Bindable] private var selectedGame:Game;
			[Bindable] private var typeOfGameSelection:int; // 1:Games en cours  |  2:Games to join
			
			//----------------------------------------------------------//
			
			public function setGames(games:ArrayCollection, typeOfGameSelection:int):void{
				this.games = games;
				this.typeOfGameSelection = typeOfGameSelection;
			}

			//----------------------------------------------------------//

			protected function init():void{
				dataGroup.addEventListener(ItemClickEvent.ITEM_CLICK, onClick);
			}

			protected function onClick(event:ItemClickEvent):void{
				selectedGame = event.currentTarget.selectedItem as Game;
			}

			//----------------------------------------------------------//
		]]>
	</fx:Script> 
	
	<s:HGroup width="100%" 
			  horizontalAlign="left"
			  height="50%">
		<s:VGroup horizontalAlign="center">
			<s:HGroup>
				<s:Label text="{games.length > 0 ? (typeOfGameSelection == 1 ? Translations.GAME_LIST.getItemAt(Session.LANGUAGE) : Translations.GAMES_TO_JOIN.getItemAt(Session.LANGUAGE)) : (typeOfGameSelection == 1 ? '(Pas de partie en cours)' : '(Pas de partie à joindre)')}"/>
				<s:Button id="refreshButton" label="{Translations.REFRESH.getItemAt(Session.LANGUAGE)}" click="{loading.visible = true; refreshButton.visible=false; typeOfGameSelection == 1 ? GameManager.getInstance().getCurrentGames() : GameManager.getInstance().getGamesToJoin()}"/>
				<mx:Spacer width="-80"/>
				<mx:SWFLoader source="{ImageContainer.LOADING}"
							  visible="false"
							  id="loading"
							  width="25"
							  height="25"/>
			</s:HGroup> 
			
			<s:Scroller maxHeight="300">
				<s:Group>
					<s:List id="dataGroup"
							itemRenderer="com.uralys.tribes.renderers.GameSimpleInfoRenderer"
							dataProvider="{games}"
							borderVisible="false"
							contentBackgroundAlpha="0"
							width="345">
						<s:layout>
							<s:VerticalLayout gap="1" useVirtualLayout="true" />
						</s:layout>
					</s:List>
				</s:Group>
			</s:Scroller>
		</s:VGroup>
		<s:VGroup width="230" 
				  paddingTop="30"
				  horizontalAlign="center"
				  visible="{selectedGame != null}">
			<renderers:GameDetailsRenderer game="{selectedGame}" 
										id="gameDetails" 
										joinable="{typeOfGameSelection == 2}"
										color="{Session.gameColor}"/>
		</s:VGroup>
	</s:HGroup>
</mx:Module>
